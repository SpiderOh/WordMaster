# Changelog

## 2026-09-02

- 初始化公开项目文档、产品规格、实施计划和开发提示词。
- 纳入 `data/reden_vocabulary_6550.csv` 作为只读导入样本。
- 初始化本地 Git 仓库并推送至 GitHub `main` 分支。
- 应用代码尚未开始实现。

## 2026-09-04

- 建立 FastAPI 后端骨架，新增 `/api/v1/health` 健康检查。
- 建立 React/Vite 前端骨架，加入移动端底部导航和 PWA manifest/service worker 占位。
- 新增 Docker Compose、前后端 Dockerfile、环境变量示例和文档入口。
- 建立 SQLite/Alembic 初始迁移，新增词库、单词、进度和操作日志模型。
- 实现 CSV 词库导入、行级错误报告、规范化重复词跳过和词库管理 API。
- 实现学习页生成、未完成页复用、跨词库跳过、熟词即时替换、完成页快照和撤销完成。
- 修复学习页事务边界：熟词替换、页面完成和撤销失败时完整回滚。
- 无可学习单词时不再持久化空页面；熟词无替补时正确标记短页。
- 当前页最后一个词标熟且无替补时将页面标记为 `exhausted`，后续不再复用空页。
- 完成撤销限制为最新记录且须在五分钟内执行，并从剩余会话恢复页面和进度时间。
- 实现 `0、1、4` 天推荐日期、整月日历摘要、按日期的学习轮次状态和不可变历史快照相邻导航。
- 新增 `/api/v1/history/calendar`、`/api/v1/history` 和 `/api/v1/history/pages/{session_id}` 接口。
- 实现遗忘事件累计与撤销、未完成页即时遗忘、熟词恢复、遗忘筛选搜索和 CSV 导出。
- 新增专攻集合状态、专攻页创建，以及记得/遗忘/熟三种专攻完成结果。
- 实现今日学习、遗忘、再次遗忘、连续学习天数和累计状态统计及明细 API。
- 实现页大小、推荐间隔、主题、字号和词库优先级设置。
- 实现版本化 JSON 全量备份恢复、事务回滚，以及单词库 CSV 导出。
- 确认外部词库 `C:\Users\zhan\Desktop\wu\output\reden_vocabulary\reden_vocabulary_6550.csv` 与仓库 `data/reden_vocabulary_6550.csv` SHA256 一致。
- 完成移动端五入口前端：学习、日期、遗忘、统计和设置，并接入现有 `/api/v1` 接口。
- 实现固定学习行、释义显隐、熟词/遗忘操作、完成确认及完成后自动续页。
- 实现学习日历状态、第二/第三次完成提示，以及按日期左右滑动浏览。
- 实现遗忘词搜索、词库/状态筛选、CSV 导出、批量选择和专攻页逐词结果提交。
- 实现主题与字号即时切换、横屏布局和 Vite 本地 API 代理。
- 新增同步记录迁移、幂等 push、游标 pull、计数增量合并、状态/设置最后写入胜出及冲突记录。
- 同步批次任一非法事件时完整回滚，并验证单词归当前用户所有。
- 新增 IndexedDB 离线事件队列、2 秒起步指数退避、联网自动重试和待同步状态提示。
- 完成 PWA 应用壳缓存、离线导航页、安装 manifest 和应用图标。
- 新增 PBKDF2 密码哈希、HMAC 访问令牌、持久化撤销、服务器模式 Bearer 保护和本地免登录模式。
- 新增服务器模式登录页、退出入口、Docker 前端 `/api/` 反向代理和单用户初始化配置。
- 生成包含认证与同步接口的 `docs/openapi.json`，补充最终本地运行、E2E smoke 和安全配置说明。
- 首次启动自动导入并启用 `data/reden_vocabulary_6550.csv`，按用户和文件名幂等，不重复覆盖已有词库。
- 简化学习单词行：移除单词下方的词库、学习次数和遗忘次数，展开释义改为显示在单词右侧。

## 2026-09-05

- 前端整体从零重写（React 18 + TypeScript + Vite 5 + react-router 6，Vitest + React Testing Library 测试栈），不复制旧实现，API 契约与业务规则保持不变。
- 移动端底部导航五入口（学习/日期/遗忘/统计/设置）+ 专攻页与词库管理二级页，统一设计变量支持深色模式、三档字号、横屏与安全区。
- 学习页：释义默认掩码并在点击后于单词左侧显示；首次学习显示“熟”、支持“遗忘”；完成本页二次确认、5 分钟内可撤销、显式“下一页”续页；短页与空词库提示。
- 日期历史页：整月日历灰色/推荐/完成三态与完成次数角标；按日查看学习轮次、第二/第三次完成与待完成轮次；快照弹窗可切换前后会话；左右滑动及按钮切换相邻日期。
- 遗忘页：服务端降序列表、防抖搜索、词库/状态/最近遗忘自筛选、CSV 导出、撤销遗忘并刷新、批量勾选生成专攻页并自动跳转；专攻页逐词记得/遗忘/熟，未选齐禁止提交。
- 统计页：今日学习/遗忘/再次遗忘（可展开明细）/连续天数与累计学习、学习中、熟词、遗忘词，全部由服务端聚合。
- 设置页：页大小、推荐间隔、主题、字号、词库优先级保存到 `/api/v1/settings`；主题与字号即时生效并本地持久化；服务器模式登录/退出；JSON 备份导出与导入。
- 词库管理页：CSV 导入（统计与行级错误展示）、激活/停用、优先级、重命名、导出、按名称确认删除。
- 离线体系重写：IndexedDB `wordmaster-offline` 双队列（遗忘=计数增量同步事件；标熟/完成=原始 API 重放，FIFO 单调序号）；撤销与专攻完成需联网。同步引擎 2 秒起步指数退避（封顶 60 秒）、联网自动重试、事件按服务端 applied/duplicate/conflict 结果清理、冲突与被拒重放记录进冲突列表；顶栏显示离线/待同步/同步冲突徽标。
- PWA：manifest、应用壳 Service Worker（静态资源缓存优先、导航回退离线页、`/api/` 不缓存）、独立离线页与 SVG 图标。
- 前端新增 Dockerfile（多阶段构建）与 nginx.conf（`/api/` 反代后端、SPA 回退、SW 不缓存）。
- 前端测试 15 个文件、100 个用例全部通过（TDD：先红后绿），类型检查与生产构建通过；后端回归 61 passed。
